# @package _global_
defaults:
  - mila.yaml

# Narval-specific overrides
hydra:
  launcher:
    # Narval has no internet access on compute nodes
    # Add any Narval-specific SLURM parameters here
    additional_parameters:
      account: rrg-lpaull
